<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpensesTracker">

        <!-- Main Activity - App entry point -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ExpensesTracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Task 10: Separate activity for month management -->
        <activity
            android:name=".MonthActivity"
            android:exported="false"
            android:label="Month Details"
            android:theme="@style/Theme.ExpensesTracker" />

        <!-- Tasks 11 & 12: Chart activity for Income/Expenses visualization -->
        <activity
            android:name=".ChartActivity"
            android:exported="false"
            android:label="Income/Expenses Chart"
            android:theme="@style/Theme.ExpensesTracker" />

    </application>

</manifest>